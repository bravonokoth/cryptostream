name: 'cryptostream'
version: '1.0.0'
config-version: 2

profile: 'cryptostream'

model-paths: ["models"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cryptostream:
    +enabled: true
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
